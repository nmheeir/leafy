import 'package:flutter/material.dart';

class SearchOlEditionScreen extends StatelessWidget {
  const SearchOlEditionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}